#!/bin/bash
# Tray entry "Restore Snapshot...": opens the Snapshots tab of HoltOS Updates,
# which lists, restores and deletes snapshots. It replaced this script's yad
# list (glass audit, 2026-09-15: yad's GTK windows cannot wear HoltOS glass).
exec /usr/local/bin/holtos-updates --snapshots
