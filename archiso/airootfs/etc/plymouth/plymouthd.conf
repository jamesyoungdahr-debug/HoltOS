# Ships our own default instead of running `plymouth-set-default-theme`
# post-install — unpackfs copies this verbatim to the target, so both the
# live medium and the installed system pick up the HoltOS theme with no
# extra Calamares step.
[Daemon]
Theme=holtos
