# HoltOS: compressed swap in RAM (zram-generator). Half of RAM, zstd —
# games and a media stack both spike; this keeps them off the disk without
# a swap partition. Managed by HoltOS (synced by the config update).
[zram0]
zram-size = ram / 2
compression-algorithm = zstd
swap-priority = 100
