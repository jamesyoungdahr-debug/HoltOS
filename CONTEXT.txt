HoltOS — running context file (plain text, kept current by the agent)
=====================================================================
Last updated: 2026-09-12 (evening; Game Mode + fixes, builds 18-20)
Read this first when resuming. Newest state at the top of each section.

WHAT HOLTOS IS
--------------
Arch-based archiso + Calamares + Limine distro with KDE Plasma 6.7.
Repo: C:\projects\holtos  (GitHub jamesyoungdahr-debug/HoltOS, PUBLIC, branch master)
Docs in repo: README.md, BUILD.md, PLAN.md, HANDOFF.md, CHANGELOG.md, BRANDING-STATUS.md
Owner: Liam (GitHub jamesyoungdahr-debug, git user Vectorly)

CURRENT STATE (2026-09-12, evening)
-----------------------------------
- PUSHED, NOT TAGGED (commit 2c431c5; Liam: "commit no release", then
  "push but dont tag"): restart prompt after system-package updates. Ships
  with the next tagged release; no ISO built for it. holtos-update-apply update_system()
  writes /run/holtos-reboot-required (reason text) when the running
  kernel's module dir is gone or pacman upgraded a core package (kernel,
  firmware, systemd, mesa, microcode, nvidia, pipewire, kwin, sddm,
  plasma-desktop) and prints "==> A restart is required...";
  holtos-update-picker shows "Restart now / Later" when the marker exists
  (Restart = systemctl reboot). Both pass bash -n. Picker edit by the
  4090's model (run_coding_task); updater edit by hand (three handoffs
  timed out: the model rewrites the whole 600-line file). Not yet seen on
  screen: the SSH-launched picker failed at pkexec (no polkit agent
  outside the session -- harness artifact); the VM (192.168.233.17) has
  both scripts installed. SEEN ON SCREEN 2026-09-12 23:48 (VM clock):
  picker from KRunner -> The Den Client -> Apply -> polkit prompt -> the
  "Done. A restart is required to finish this update: <reason>. Restart
  now?" dialog with Restart now / Later. VM address is now 172.25.64.50 (it rebooted at 21:14, which also
  proved the /run marker clears on reboot).
- the-den-client icon (v0.4.6): app_id is right now (KWin: class
  the-den-client, desktopFile the-den-client) but src/assets/logo.svg is
  invalid XML ("--" inside the comment on line 2), so Qt renders it
  empty -> no window/task icon. Client-side fix + new tag needed.
- Local workers: 4090 = qwen3-coder-30b-a3b-instruct, tool calls verified.
  4080 = tool calls worked with qwen2.5-14b-instruct but it hallucinated a
  value it should have read; at 21:00 the 4080 answered 400 (no model
  loaded) -- Liam says a new model is coming; retest with the list/read/
  write check. Another agent may be working alongside; check git status
  and the LM Studio queue before handing off.
- RELEASED v0.0.4-alpha (2026-09-12 ~19:45, commit de7f1fc): GitHub
  pre-release with notes; build 21 = release ISO (out/). Upgrade path
  verified on the build 20 VM install: `holtos-update-apply config the-den
  the-den-client` -> config v0.0.4-alpha (updater hands over to the
  release's copy), The Den v0.6.1, client v0.4.6 (icons + desktop file
  refreshed). Installed 0.0.3-alpha systems need the curl one-liner from
  the release notes first (old whitelist). Liam no longer uses the Ventoy
  stick: releases reach his machines through the updater (no more ISO
  copies to D:\).
- Builds 18-20 (2026-09-12 evening): Game Mode (gamescope + Steam Big
  Picture session, SteamOS-style switch both ways, tray toggle), greeter
  session picker, installed-system motd, updater refreshes The Den Client
  launcher/.desktop/icons on every update, chromium in the image (The Den
  v0.5.1 needs it), live-medium udev rule (no "Disks & Devices" popup over
  Calamares), SDDM state seeded with Plasma on install. Build 18 install
  verified in the VM; Game Mode there fails (no GPU) and bounces back to
  Plasma automatically, which is the designed failure path. Build 20 fresh
  install verified too: no Disks & Devices popup over Calamares, login
  screen preselects Plasma. The real Game
  Mode screen needs Liam's Strix Halo box. Build 20 = everything above.
  Next: copy build 20 ISO to D:\ (stick not plugged in), tag v0.0.4-alpha
  after the real-hardware check.
- VM: SSH key is scratchpadmkey (user holtos, NOPASSWD sudo on the test
  install); address moves with the Default Switch subnet (was
  192.168.233.17 at 19:00). The adapter once came up disconnected from the
  switch -- Connect-VMNetworkAdapter -SwitchName "Default Switch" fixes it.
- LM Studio: deepseek-coder-v2-lite-instruct cannot tool-call, so the
  bridge's run_coding_task never writes files; chat_completion works.
- Dev machine (LiamPC, Ryzen 7950X, 64 GB) was rebuilt from nothing today:
  Git 2.55, GitHub CLI 2.100 (Liam signed in), WSL2 Ubuntu 26.04 + podman
  5.7 (user liam, passwordless sudo, .wslconfig caps 24 threads / 32 GB),
  Hyper-V + VM holtos-test (Gen 2, 6 vCPU, 8 GB, 60 GB VHDX in vm/,
  Secure Boot off, Default Switch). Liam is in Hyper-V Administrators; UAC
  is off, elevated scripts run via Start-Process -Verb RunAs.
- Build 9 = first ISO from this machine: out/holtos-0.0.1-alpha-x86_64.iso
  (2026-09-12 03:35 local). Copied to D:\ (the Ventoy stick) as
  holtos-0.0.1-alpha-x86_64-build9.iso; older copy removed (Liam: always
  replace the old ISO on D:\).
- Build 9 VERIFIED in the VM, live and installed (see HANDOFF.md):
  glass forks active (holtosglass effect, org.holtos.glass decoration),
  About this System branded, live-ISO guards, no failed units, The Den
  active, Btrfs @ + zstd, keyring OK. Bugs found: no Display page
  (kscreen missing — the "no resolution options" report), no volume
  applet backend (pipewire-pulse), powerprofilesctl traceback
  (python-gobject), live session auto-locks, partition legend still
  dark-on-dark (Calamares hardcodes the pens; now on a light chip).
- Build 10 (07:27 local) VERIFIED on a second fresh install: Display
  page present (kscreen), volume applet, Bluetooth/Login/RDP pages,
  powerprofilesctl OK, live session no longer locks, legend chip
  readable, 0 failed units. Version bumped to 0.0.2-alpha, tagged, and
  build 11 rebuilt from the tag as the release ISO (PLAN step 5).
- Repo is PUBLIC as of 2026-09-12 (Liam's call). Updater verified end to
  end on the build 10 install: check -> apply config (snapshot + release
  tarball) -> up to date (exit 1).

AFTERNOON 2026-09-12 (unreleased, builds 12-15, all VM-verified)
------------------------------------------------------------------
- Snapshot restore: holtos-btrfs-restore (root; <name> | --current |
  --cleanup), tray "Restore Snapshot...", autostart notice on snapshot
  boots, holtos-btrfs-restore-cleanup.service deletes @.old-* at boot.
  NEVER btrfs-delete the running root (it works and kills the session).
- Other OSes in Limine: holtos-limine-other-os (install, kernel-update
  hook, tray "Rescan Boot Menu"); entries between HOLTOS OTHER OS markers.
  Test rig: second VHDX vm/fake-windows.vhdx with EFI/Microsoft/Boot/
  bootmgfw.efi + EFI/ubuntu/grubx64.efi (both copies of Limine).
- Limine theme: set in homelab-limine-install.sh; font from terminus-font
  converted in customize_airootfs.sh to /usr/share/holtos/limine/
  ter-124b.bin, copied to ESP /holtos/. Snapshots live in a collapsed
  /Snapshots submenu written by holtos-btrfs-snapshot.
- yad 15: no --question / --info. Plain `yad --text ... --button` only.
- Limine menu screenshots need a long timeout: sed timeout: 30 into both
  limine.conf copies on the ESP, reboot, shot after ~24 s. Holding Down
  via TypeKey during boot is unreliable.
- v0.0.3-alpha tagged + released (commit f75196f); ISO build 16 from it in
  out/. D:\ was unplugged when builds 15/16 finished — build 14 is what is
  on the stick unless Liam copies again.
- Updater: config update syncs the component whitelist in
  holtos-update-apply (update_config); add new HoltOS-owned paths there.
  Package repo: GitHub release "packages" = local-repo/ contents; publish
  with tools/publish-packages.sh after build-local-repo.sh. Installed
  systems get [homelab] from usr/share/holtos/pacman-homelab.conf.
- 0.0.2 installs need the one-liner in the v0.0.3 release notes first.
- Liam's next machine: AMD Strix Halo (RDNA 3.5). Nothing extra needed;
  check hardware.log (vainfo/vulkaninfo) and Display page HDR/VRR there.
- FOCUS: media + gaming/entertainment. Roadmap in
  docs/holtos-must-haves-and-plan.md (gaming base -> Game Mode session ->
  HoltOS Apps store over Flathub -> controllers/CEC -> media player +
  storage wizard). Network Shares tool done (holtos-share/holtos-shares).

FORKS (holtos-glass-effect, holtos-window-decoration)
-----------------------------------------------------
Both live in forks/ and are built by build-local-repo.sh into local-repo/
(packaging/*/PKGBUILD, version suffix = commit count: r54 today). Verified
on screen in build 9. The decoration-frame fix (force-blur the frame even
when KWin reports the decoration as opaque) is in forks/ and shipped.
LEFTOVER: two public GitHub repos created before the monorepo decision
(jamesyoungdahr-debug/holtos-glass-effect, /holtos-window-decoration)
still exist — delete with `gh repo delete` (needs the delete_repo scope,
so Liam runs it).
Live-tuning recipe in the VM: kwriteconfig6 --file kwinrc --group
Effect-holtosglass --key <Key> <value>; qdbus6 org.kde.KWin /Effects
reconfigureEffect holtosglass. Kvantum opacity: copy the theme to
~/.config/Kvantum/HoltOSGlass/ and set reduce_window_opacity=N.

DISPLAY PAGE / HDR / VRR (Liam's question, 2026-09-12)
-----------------------------------------------------
System Settings > Display was missing outright: kscreen (the KCM) was not
in packages.x86_64. Fixed in build 10. Beyond that, Plasma 6 shows
resolution/refresh/scale always, and HDR / VRR / wide gamut only when the
DRM driver reports the capability: hyperv_drm reports none (VM), AMD/Intel
get them from the Mesa/amdgpu/i915 stack already in the image, NVIDIA
needs the driver installed by hardware detection (PLAN step 7, real
machine only) plus KWIN_DRM_ALLOW_NVIDIA_COLORSPACE=1 for the HDR toggle
on Plasma >= 6.2 (KDE hid it on NVIDIA behind that env var because of a
login-blocking driver bug; not shipped by default — enable per machine).
GPU-PV / dxgkrnl in Hyper-V cannot test any of this: it gives /dev/dxg
(compute) with no DRM device, no NVIDIA PCI device, no nvidia.ko.

OPEN DECISIONS (Liam)
---------------------
- Snapshot semantics (rescue boot vs rollback).
- Real NVIDIA machine test for hardware detection (PLAN step 7) and the
  real-GPU look of the glass: boot build 10+ from the Ventoy stick (D:\).
- Delete the two orphan fork repos on GitHub.

HOW TO DRIVE THE TEST VM (Hyper-V "holtos-test")
-------------------------------------------------
Tooling lives in the session scratchpad (C:\Users\Liam\AppData\Local\Temp\
claude\C--projects-holtos\<session>\scratchpad\): vm.ps1 (non-elevated
wrapper) runs vmctl.ps1 elevated. Actions: state | iso=<path>|none |
start | off | shutdown | bootdisk | bootdvd | net=on|off | shot=<png> |
type=<text> | key=<vk> | keydown/keyup=<vk> | holdkey=<vk>,<secs> |
click/dblclick/rclick/move=<x>,<y> | sleep=<s>. Screenshots come from
Msvm_VirtualSystemManagementService.GetVirtualSystemThumbnailImage at the
guest's real resolution (1024x768); keyboard via Msvm_Keyboard TypeKey
per character (VkKeyScan + Shift); mouse via Msvm_SyntheticMouse
SetAbsolutePosition + SetButtonState (Click() alone does nothing).
Synthetic clicks do NOT activate taskbar/desktop icons: open apps with
KRunner (keydown=18 key=113 keyup=18, type name, key=13). Then SSH
(key: scratchpad/vmkey): in Konsole type
  sudo systemctl start sshd && mkdir -p ~/.ssh && echo '<pub>' > ~/.ssh/authorized_keys
The VM's address changes when the host's Default Switch renews its subnet
(172.30.x -> 172.29.x after the 2026-09-12 host reboot); read it off a
Konsole screenshot (`ip -4 -br a`) — Get-VMNetworkAdapter shows nothing
(no KVP daemon) and the host neighbour cache also lists WSL.
Live session user liveuser (passwordless sudo). Installed test user
holtos / password holtos — sudo over ssh needs `echo holtos | sudo -S`,
and three wrong attempts trip pam_faillock for 10 minutes.
Installer at 1024x768: Next (977,145) on early pages, (977,61) once the
window grows on Summary; Erase disk radio (20,254); Users fields: name
(110,233), password (110,434), repeat (316,434), same-password box
(19,494). Erase-disk install of build 9 took under 3 minutes.
guest-checks.sh (scratchpad) is the in-guest verification script; run it
with `ssh ... 'bash -s' < guest-checks.sh`.
The DVD stays attached: boot order decides (bootdisk / bootdvd).
